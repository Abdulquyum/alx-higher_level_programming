#!/usr/bin/python#
def add_tuple(tuple_a=(), tuple_b=()):
    tuple_c = ()
    if len(tuple_a) < 2:
        tulple_a[1] = 0
    a = tuple_a[0] + tuple_b[0]
    b = tuple_a[1] + tuple_b[1]

    tuple_c.append(a)
    tuple_c.append(b)
